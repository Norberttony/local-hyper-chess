import { VariationMove } from "./index.js";
export declare const VALID_HEADERS: readonly ["Event", "Site", "Round", "TimeControl", "White", "Black", "Result", "Termination", "Variant", "FEN"];
export type PGNHeader = typeof VALID_HEADERS[number];
export type PGNHeaders = {
    [key in PGNHeader]?: string;
};
export declare class PGNData {
    pgnRoot: VariationMove;
    startedWTP: boolean;
    headers: PGNHeaders;
    constructor(pgnRoot: VariationMove);
    private initHeaders;
    clearHeaders(): void;
    clear(): void;
    setHeader(hdr: PGNHeader, value: string): void;
    unsetHeader(hdr: PGNHeader): void;
    toString(): string;
    sanHelper(node: VariationMove | undefined, count: number): string;
    get san(): string;
}
//# sourceMappingURL=pgn-data.d.ts.map