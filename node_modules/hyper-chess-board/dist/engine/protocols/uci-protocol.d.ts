import { BotProtocol } from "../abstract/protocol.js";
import type { BotProcess } from "../abstract/bot-process.js";
import { GameTime } from "../utils.js";
export declare class UCIBotProtocol extends BotProtocol {
    #private;
    private startFEN;
    private moves;
    constructor(botProcess: BotProcess);
    setFEN(fen: string): void;
    playMove(lan: string): void;
    thinkForMoveTime(ms: number, allowTimeout?: boolean, timeoutPaddingMs?: number): Promise<string | undefined>;
    thinkTimedGame(time: GameTime, allowTimeout?: boolean, isWhite?: boolean, timeoutPaddingMs?: number): Promise<string | undefined>;
    thinkForDepth(depth: number): Promise<string | undefined>;
    startThink(): void;
    stopThink(): void;
    isReady(timeoutMs?: number): Promise<boolean>;
}
//# sourceMappingURL=uci-protocol.d.ts.map