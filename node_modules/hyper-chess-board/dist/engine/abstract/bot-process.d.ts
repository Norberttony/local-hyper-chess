export type onReadLineListener = (line: string) => any;
export declare abstract class BotProcess {
    #private;
    private log;
    private readListeners;
    private promptPrefix?;
    private onPromptSuccess?;
    private promptTimeout?;
    constructor();
    abstract get isRunning(): boolean;
    abstract start(): void;
    abstract stop(): void;
    addReadLineListener(listener: onReadLineListener): void;
    write(cmd: string): void;
    restart(): void;
    protected readLine(line: string): void;
    prompt(cmd: string, prefix: string, timeoutMs?: number | undefined): Promise<string>;
}
//# sourceMappingURL=bot-process.d.ts.map