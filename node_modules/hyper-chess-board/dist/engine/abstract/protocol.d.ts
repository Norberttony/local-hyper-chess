import type { GameTime, ThinkStats } from "../utils.js";
import type { BotProcess } from "./bot-process.js";
type ThinkStatsUpdateListener = (stats: ThinkStats) => any;
export declare abstract class BotProtocol {
    protected bot: BotProcess;
    protected thinkStats: {
        [depth: number]: ThinkStats;
    };
    protected currDepth: number;
    private thinkStatsUpdateListeners;
    private engineName;
    private authorName;
    constructor(botProcess: BotProcess);
    protected setEngineName(name: string): void;
    protected setAuthorName(name: string): void;
    getEngineName(): string;
    getAuthorName(): string;
    addThinkStatsUpdateListener(listener: ThinkStatsUpdateListener): void;
    protected updateThinkStats(stats: ThinkStats): void;
    getThinkStats(): ThinkStats;
    resetThinkStats(): void;
    abstract setFEN(fen: string): void;
    abstract playMove(lan: string): void;
    abstract thinkForMoveTime(ms: number, allowTimeout: boolean, timeoutPaddingMs: number): Promise<string | undefined>;
    abstract thinkTimedGame(time: GameTime, allowTimeout: boolean, isWhite: boolean, timeoutPaddingMs: number): Promise<string | undefined>;
    abstract thinkForDepth(depth: number): Promise<string | undefined>;
    abstract startThink(): void;
    abstract stopThink(): void;
    abstract isReady(timeoutMs: number): Promise<boolean>;
}
export {};
//# sourceMappingURL=protocol.d.ts.map