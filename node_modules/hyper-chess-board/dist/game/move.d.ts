import { Piece } from "./piece.js";
export interface Capture {
    sq: number;
    captured: Piece;
}
export declare class Move {
    to: number;
    from: number;
    captures: Capture[];
    constructor(to: number, from: number, captures?: Capture[]);
    clone(): Move;
    get lan(): string;
}
//# sourceMappingURL=move.d.ts.map